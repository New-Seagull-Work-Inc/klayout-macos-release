---
name: Bug report
about: A board that routed wrong, a crash, a wrong verdict, or a Cinema problem
title: ""
labels: bug
assignees: ""
---

**Build**
Paste the output of `./klayout --version`:

```
```

**Command line**
The exact command you ran (with `--layers`, `--plane`, `--fab`, seeds, …):

```
```

**What happened / what you expected**


**The log**
Attach `klayout.log` from the output directory (drag it in), or paste
the last 60 lines — the trial table and the verdict are at the end:

```
```

**The board**
Can you share the input project (the `.kicad_pcb`, `.kicad_pro`,
`.kicad_sch` and libraries)? Attach a zip, or say if it is confidential
— a minimal board that reproduces the problem is ideal.

**Environment**
- macOS version and chip (Apple Silicon / Intel):
- KiCad version (`kicad-cli version`), if installed:
- Browser, for PCB Layout Cinema problems:
